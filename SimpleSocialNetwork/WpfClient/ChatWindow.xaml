<Window x:Class="WpfClient.ChatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfClient"
        mc:Ignorable="d"
        Title="ChatWindow" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        
        <ListBox x:Name="listbox_chat_log" Grid.Row="0" SelectionMode="Single">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Height="30px">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Username}" Foreground="Black" Grid.Column="0" Margin="5 0 0 0" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding Message}" Foreground="Black" Grid.Column="1" Margin="5 0 0 0" VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <TextBox x:Name="txtbox_chat" Grid.Row="1" Margin="0 0 50 0" />
        <Button 
            x:Name="btnSubmit" 
            Grid.Row="1" 
            Width="50" 
            HorizontalAlignment="Right"
            Click="btnSubmit_Click">
            Send
        </Button>
    </Grid>
</Window>
